name: navbar
description: "A customizable and responsive navigation bar for Flutter applications."
version: 0.0.1
repository: https://github.com/Al-Fuad/Packages/flutter/navbar

topics: 
  - bottom-nav
  - bottom-navigation
  - navigation
  - nav
  - navbar

environment:
  sdk: ^3.11.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

platforms:
  android:
  ios:
  web:
  macos:
  windows:
  linux:

